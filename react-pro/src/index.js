import React from 'react';
import ReactDOM from 'react-dom/client';

// 默认使用演示切换器
import App from './DemoSwitcher';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
